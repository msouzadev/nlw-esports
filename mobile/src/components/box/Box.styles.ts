import styled from 'styled-components/native';
import { space, color, position, border, layout, flexbox } from 'styled-system';

export const Container = styled.View`
  ${color}
  ${space}
  ${layout}
  ${position}
  ${border}
  ${flexbox}
`;
